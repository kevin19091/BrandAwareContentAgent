APP_USER=admin
APP_PASS=changeme

# Set to true to run every pipeline node on canned output, no LLM calls or
# network required (see docs/tasks.md M4).
USE_MOCK=true

OPENAI_API_KEY=
ANTHROPIC_API_KEY=

PORT=7860
